name: SublimationService
settings:
  LINT_MODE: ${LINT_MODE}
packages:
  SublimationService:
    path: .
aggregateTargets:
  Lint:
    buildScripts:
      - path: Scripts/lint.sh
        name: Lint
        basedOnDependencyAnalysis: false
    schemes: {}
